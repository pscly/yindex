{
  "name": "yindex",
  "private": true,
  "version": "0.1.3",
  "workspaces": ["packages/*", "packages/examples/*"],
  "scripts": {
    "dev": "bun run --filter @yindex/extension dev",
    "build": "bun run --filter @yindex/extension build",
    "test": "bun test packages/domain packages/widget-sdk packages/style-packs packages/widgets packages/extension",
    "typecheck": "bun run --filter @yindex/domain typecheck && bun run --filter @yindex/widget-sdk typecheck && bun run --filter @yindex/style-packs typecheck && bun run --filter @yindex/widgets typecheck && bun run --filter @yindex/extension typecheck",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test:e2e": "playwright test",
    "verify": "bun run test && bun run typecheck && bun run lint && bun run build && bun run test:e2e",
    "pack": "bash scripts/pack-extension.sh",
    "release:zip": "bash scripts/pack-extension.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@playwright/test": "1.58.2",
    "@types/bun": "^1.2.0",
    "typescript": "^5.8.2"
  }
}
