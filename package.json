{
  "name": "yindex",
  "private": true,
  "version": "0.3.0",
  "workspaces": [
    "packages/*",
    "packages/examples/*"
  ],
  "scripts": {
    "dev": "bun run --filter @yindex/extension dev",
    "build": "bun run --filter @yindex/extension build",
    "test": "bun test packages/domain packages/widget-sdk packages/style-packs packages/widgets packages/extension",
    "typecheck": "bun run --filter @yindex/domain typecheck && bun run --filter @yindex/widget-sdk typecheck && bun run --filter @yindex/style-packs typecheck && bun run --filter @yindex/widgets typecheck && bun run --filter @yindex/extension typecheck && bunx tsc -p tsconfig.e2e.json --noEmit",
    "lint": "biome check .",
    "format": "biome check --write .",
    "test:e2e": "playwright test",
    "verify": "bun run test && bun run typecheck && bun run lint && bun run build && bun run test:e2e && bun run pack",
    "pack": "bash scripts/pack-extension.sh",
    "release:zip": "bash scripts/pack-extension.sh",
    "preview": "bun run build && bun scripts/capture-previews.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@biomejs/biome": "^1.9.4",
    "@playwright/test": "1.58.2",
    "@types/bun": "^1.2.0",
    "@types/pngjs": "^6.0.5",
    "pngjs": "^7.0.0",
    "typescript": "^5.8.2"
  }
}
